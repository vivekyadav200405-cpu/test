/* ============================================================
   QUESTION POOL — extras  (intentionally EMPTY)
   ------------------------------------------------------------
   The question bank is now built ONLY from the actual training
   content (see question_pool.js). The earlier large set of
   generic/extra questions was removed on purpose so that every
   question comes from the topics taught in "zillionsoftech".

   To add more questions, either append them in question_pool.js
   or add them from the Admin → Questions Manager (stored in the
   database and merged automatically).
   ============================================================ */
(function () {
    // no extra questions — kept as a no-op so existing <script> tags
    // and load order do not break.
})();
